{% extends 'base.html.twig' %}



{% block body %}
    <h1>Modification du véhicule</h1>

    {{ include('vehicule/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('vehicule_index') }}">Annuler</a>


{% block body %}
    {% if app.user %}
        {% if app.user.roles[0] == 2 %}
            <h1>Edit Vehicule</h1>
            {{ include('vehicule/_form.html.twig', {'button_label': 'Update'}) }}
            <a href="{{ path('vehicule_index') }}">back to list</a>
            {{ include('vehicule/_delete_form.html.twig') }}
        {% else %}
            <h1> Vous n'êtes pas autorisé à visualiser cette page </h1>
        {% endif %}
    {% else %}
        <h1> Vous n'êtes pas autorisé à visualiser cette page </h1>
    {% endif %}
{% endblock %}
